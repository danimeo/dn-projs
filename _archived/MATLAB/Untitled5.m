N = [1e1 1e2 1e3 1e4];  % 四个数：10, 100, 1000, 10000

p = 0;  % 累加变量p初始化为0
for i = 1:max(N)  % 循环变量i的值从1增加到最大数10000
    p = p + 1 / i^2;  % 计算迭代至此时累加的结果
    if ismember(i, N)  % 当i是四个数之一时
        disp(sqrt(p * 6));  % 输出对应求得的π值
    end
end
